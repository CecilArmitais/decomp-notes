# `e029b3cb` — Decomp the dungeon RNG mode setters and nine sound helpers

| | |
|---|---|
| **Commit** | `e029b3cb` (third of five) |
| **Branch** | `decomp-continued`, on top of `14d9204c` |
| **Verified** | matching build, `build/pmdsky.us/pmdsky.us.nds: OK` |

> **Unverified AI-authored reasoning.** Not part of the decompilation, never
> merged, not authoritative. The diff and the matching build are the sources of
> truth — see [the README](../README.md).

## No new types were needed

**Fact.** `DungeonRngSetPrimary`, `DungeonRngSetSecondary` and
`DungeonRngUnsetSecondary` write `prng_state::use_secondary` (offset 0) and
`::idx_secondary` (offset 0x10). Both the struct and the
`extern prng_state DUNGEON_PRNG_STATE` already existed — `include/dg_random.h`
and `src/dg_random.c`. The asm offsets land exactly on those members, which is
independent corroboration of the existing layout.

**Fact.** `sub_02017C74` keeps the signature `src/overlay_25_init.c` already
declares, rather than a fresh guess — the conflicting-declaration trap this
branch has hit before.

## The sound helpers

**Fact.** Five tail-call `sub_02017C50` with a fixed id — `PlayLevelUpSound`
passes 1, both `PlayDungeonTipSound__*` pass 7, `ov29_022EAC9C` passes 0 and
`ov29_022EACAC` passes 5. Two guard on a sentinel first:
`PlaySeByIdIfNotSilence` on `0x3F00` and `PlayMeByIdIfNot998` on 998 — the
latter matching its pmdsky-debug name exactly, which is a small check that the
reading is right.

**Inference.** `sub_02017C50` is undeclared; `void f(s32 id)` is written from
the call sites, where every caller passes one integer.
