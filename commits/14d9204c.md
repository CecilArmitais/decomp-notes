# `14d9204c` — Decomp the last ten simple move-effect wrappers in overlay_29

| | |
|---|---|
| **Commit** | `14d9204c` (second of five) |
| **Branch** | `decomp-continued`, on top of `80e48105` |
| **Verified** | matching build, `build/pmdsky.us/pmdsky.us.nds: OK` |

> **Unverified AI-authored reasoning.** Not part of the decompilation, never
> merged, not authoritative. The diff and the matching build are the sources of
> truth — see [the README](../README.md).

**Fact.** This exhausts the simple wrapper family. Three depart from the
template, and each is a genuine finding rather than a spelling choice:

- **`DoMoveInvisify` passes the attacker as both arguments** (`mov r1, r0`), so
  Invisify targets the user, not the defender. Written
  `TryInflictInvisibleStatus(attacker, attacker)`.
- **`DoMoveGrudge` and `DoMoveDamageStealItem` are tail calls**
  (`ldr ip, =f` / `bx ip`), written `return f(...)`. MWCC reproduces the tail
  jump from that form. `DoMoveDamageStealItem` forwards its own four arguments
  unchanged to `DoMoveTakeaway`, which is still asm.
- **`DoMoveMetalBurst` passes 15 to `SetReflectStatus`** — a third value for
  that selector after 4 (Counter) and 10 (Rebound).

**Inference.** `TryInflictGrudgeStatus` is declared returning `bool8` here only
because `DoMoveGrudge` returns its result; nothing establishes the type.
