# `b1b5c94f` — Use generic local names in the five functions landed since 51c365db

| | |
|---|---|
| **Commit** | `b1b5c94f` (as of writing — renamed if amended or rebased) |
| **Branch** | `decomp-continued`, on top of `abfcb6e1` |
| **Verified** | all three ROMs after the rename: `pmdsky.us.nds: OK`, `pmdsky.eu.nds: OK`, `pmdsky.jp.nds: OK` |

> **Unverified AI-authored reasoning.** Not part of the decompilation, never
> merged, not authoritative. The PR diff and the matching build are the sources
> of truth — see [the README](../README.md).

---

A rename-only commit over `CalcDamage` ([`abfcb6e1`](abfcb6e1.md)),
`CalcTypeBasedDamageEffects` ([`d364822d`](d364822d.md)), `ApplyDamage`
([`a51777ff`](a51777ff.md) / [`ce359434`](ce359434.md)), `ApplyDamageAndEffects`
([`cd5c7d56`](cd5c7d56.md)) and `ov11_022ED69C` ([`afd96d2b`](afd96d2b.md)).

**Why.** Those functions' locals carried names the decompiler had inferred
(`off_ability_mult`, `has_status`, `bplHeader`, …). A local's meaning is the one
thing the matching build cannot check, and the earlier notes already flag such
names as inference. They are replaced by type-derived generic names numbered in
declaration order: `fp1..` (fixed-point temporaries), `mon1..` (monster
pointers), `v1..` (scalars), `f1..` (`bool8` flags), `p1..`/`ent1..`/`item1..`/
`ls1..` (other pointers), `arr`/`buf` (arrays). Names that were already generic
(`i`, `j`, `k`, `n`, `result`) or are simply the type's own name (`diag`,
`weather`, `tile`, `member`, `entry`, `spec`) are unchanged.

**What is deliberately unchanged.** Parameters — they are header-level, several
mirror existing tree declarations, and upstream itself syncs parameter names
from pmdsky-debug (`cf1fa6f3` on this same branch). Declaration order — it fixes
the frame layout; only identifiers changed.

**Fact.** A token-level comparison of the diff (831 changed line pairs) shows
the only differing tokens are the mapped old/new identifiers; no line changed
structure. Identifiers do not reach code generation, and the three-region
builds confirm the objects are unchanged.

**For a reviewer.** The earlier notes' code snippets use the old names; read
them as the same code. If upstream prefers descriptive locals, the map is the
diff itself and is mechanically reversible.
