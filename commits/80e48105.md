# `80e48105` — Decomp ten more move-effect wrappers in overlay_29

| | |
|---|---|
| **Commit** | `80e48105` (first of a five-commit run of fifty functions) |
| **Branch** | `decomp-continued`, on top of `edd80366` |
| **Verified** | matching build, `build/pmdsky.us/pmdsky.us.nds: OK` |

> **Unverified AI-authored reasoning.** Not part of the decompilation, never
> merged, not authoritative. The diff and the matching build are the sources of
> truth — see [the README](../README.md).

**Fact.** DoMoveShocker, DoMoveOneRoom, DoMoveHurl, DoMoveMobile, DoMoveSeeStairs,
DoMoveLongToss, DoMovePierce, DoMoveAquaRing, DoMoveGastroAcid, DoMoveLuckyChant
— the forward-and-return-`TRUE` template, all ten matched first try.

**Inference.** All ten helpers are undeclared in the tree; prototypes written
from call sites, `void` returns unverifiable because the wrappers discard the
result, and the two `(TRUE, FALSE)` argument names are positional guesses. Same
caveats as every batch in this family.
